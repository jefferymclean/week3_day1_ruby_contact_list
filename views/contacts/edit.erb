<h1>Edit Contact</h1>
<form action="/contacts/<%= @contact.id %>" method="contact">
  <input type="hidden" name="_method" value="put"/> 
 
  <label for="contact_title">Title:</label><br/>
  <input id="contact_name" name="contact[name]" type="text" value="<%= @contact.name %>"/>
  <br/>
 
  <label for="contact_body">Body:</label><br/>
  <textarea id="contact_email" name="contact[email]" rows="5"><%= @contact.email %></textarea>
  <br/>
 
  <input type="submit" value="Edit Contact"/>
</form>   